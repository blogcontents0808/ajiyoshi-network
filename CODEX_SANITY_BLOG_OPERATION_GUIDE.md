# 🤖 CODEX専用 Sanityブログ運用マニュアル

**味美ネットワーク ウェブサイト - CODEX運用用詳細ガイド**

---

## 🎯 CODEXがSanityブログで行う作業

### 📝 日常的な作業
1. **ブログ記事作成・編集**
2. **画像アップロード・最適化**
3. **カテゴリー管理**
4. **公開スケジュール管理**
5. **データ同期・修復**

---

## 🚀 Sanity Studio 操作手順

### 1. 起動方法
```bash
# メインプロジェクトディレクトリに移動
cd "D:\AI-SANDBOX\ajiyoshi-network-clean"

# Sanity Studio起動
cd vibe-blog-by-sanity
npm run studio

# 起動確認: http://localhost:3333
```

### 2. ログイン・認証
- **URL**: http://localhost:3333
- **認証**: Sanity アカウントでログイン
- **権限**: 管理者権限で全機能アクセス可能

---

## ✏️ ブログ記事作成の詳細フロー

### 📋 新規記事作成手順

#### ステップ1: 基本情報入力
```json
{
  "title": "記事タイトル（必須）",
  "slug": "url-friendly-slug",
  "publishedAt": "2025-11-07T10:00:00.000Z",
  "category": "活動報告",
  "excerpt": "記事の要約（SEO用）"
}
```

#### ステップ2: アイキャッチ画像設定
1. **「mainImage」フィールドをクリック**
2. **画像アップロード方法**:
   - ドラッグ&ドロップ
   - ファイル選択ダイアログ
3. **画像設定**:
   - **Alt Text**: 必須（アクセシビリティ）
   - **Caption**: オプション（説明文）

#### ステップ3: 記事本文作成（PortableText）

**基本的な要素**:
```markdown
# 見出し1（H1）
## 見出し2（H2）
### 見出し3（H3）

通常の段落テキスト。

**太字テキスト**
*斜体テキスト*

- 箇条書きリスト
- 項目2
- 項目3

1. 番号付きリスト
2. 項目2
3. 項目3
```

**画像の挿入**:
1. 本文中で「+」ボタンクリック
2. 「Image」を選択
3. 画像アップロード
4. Alt text設定（必須）

---

## 🔧 高度な編集機能

### 📊 カスタムブロック
```json
{
  "type": "callout",
  "content": "重要なお知らせ",
  "style": "info|warning|success|error"
}
```

### 🔗 リンク設定
- **内部リンク**: サイト内ページ
- **外部リンク**: 他サイトへのリンク
- **メールリンク**: mailto:形式

### 📅 公開設定
- **即座に公開**: `publishedAt`を現在時刻に設定
- **予約公開**: 未来の日時を設定
- **下書き保存**: `publishedAt`を空にする

---

## 🖼️ 画像管理システム

### 📱 アップロード仕様
- **対応形式**: JPG, PNG, WebP, GIF
- **最大サイズ**: 10MB
- **推奨サイズ**: 幅800-1200px
- **圧縮**: Sanity CDNが自動最適化

### 🎨 画像最適化
```javascript
// Sanity Image URL API
import imageUrlBuilder from '@sanity/image-url'

const builder = imageUrlBuilder(client)

function urlFor(source) {
  return builder.image(source)
    .width(800)
    .height(600)
    .quality(80)
    .format('webp')
}
```

### 📝 Alt Text Guidelines
- **説明的**: 画像の内容を具体的に記述
- **簡潔**: 125文字以内
- **文脈考慮**: 記事内容との関連性

---

## 🔄 データ同期・管理

### 🚀 自動同期システム
```bash
# 基本同期コマンド
cd "D:\AI-SANDBOX\ajiyoshi-network-clean"
node complete-sanity-sync.js

# 新規投稿のみ同期
node sync-new-posts.js

# 定期同期システム起動
npm run sync:schedule
```

### 🔍 データ整合性チェック
```bash
# 全投稿データチェック
node check-all-posts-proper.js

# 特定記事のデバッグ
node debug-specific-post.js [slug]

# 画像リンクチェック
node check-image-links.js
```

### 🛠️ トラブル時の修復
```bash
# 緊急データ修復
node fix-blog-data.js

# ブログ記事再生成
node regenerate-blog-posts.js

# 画像パス修復
node fix-image-paths.js
```

---

## 📊 コンテンツ管理ベストプラクティス

### ✅ 記事作成チェックリスト
- [ ] タイトルは魅力的で検索しやすい
- [ ] スラッグはURL適正（英数字、ハイフン）
- [ ] アイキャッチ画像は高品質
- [ ] Alt textは適切に設定
- [ ] 本文は読みやすい構造
- [ ] 見出しは適切な階層
- [ ] 公開日時は正確
- [ ] カテゴリーは適切

### 🎯 SEO最適化
```json
{
  "meta": {
    "title": "30-60文字の魅力的なタイトル",
    "description": "120-160文字の要約",
    "keywords": ["味美", "ネットワーク", "地域", "活動"],
    "ogImage": "SNSシェア用画像URL"
  }
}
```

### 📱 レスポンシブ対応
- **画像**: 自動的に最適サイズ配信
- **テキスト**: モバイル読みやすさ重視
- **レイアウト**: デバイス別最適表示

---

## 🔍 記事パフォーマンス分析

### 📈 追跡すべき指標
- **公開記事数**: 現在の総数
- **平均文字数**: 読みやすさ指標
- **画像使用率**: 視覚的魅力度
- **カテゴリー分布**: コンテンツバランス

### 📊 現在の状況
```json
{
  "totalPosts": 8,
  "averageWordCount": 1220,
  "totalImages": 18,
  "categories": {
    "活動報告": 6,
    "お知らせ": 2
  },
  "lastUpdated": "2025-11-07"
}
```

---

## 🚨 緊急時対応マニュアル

### ❌ よくあるエラーと解決法

#### 1. 記事が表示されない
```bash
# 症状確認
node check-all-posts-proper.js

# データ修復
node fix-blog-data.js

# 同期再実行
node complete-sanity-sync.js
```

#### 2. 画像が表示されない
```bash
# 画像リンクチェック
node check-image-links.js

# CDN接続確認
curl https://cdn.sanity.io/images/qier3tei/production/

# 画像パス修復
node fix-image-paths.js
```

#### 3. Sanity Studio 接続エラー
```bash
# 認証トークン確認
echo $SANITY_API_TOKEN

# 依存関係再インストール
cd vibe-blog-by-sanity
npm install

# Studio再起動
npm run studio
```

#### 4. 同期システムエラー
```bash
# ログファイル確認
cat sync-error.log

# データベース接続テスト
node test-sanity-connection.js

# 完全リセット同期
node complete-data-reset.js
```

---

## 🔐 セキュリティ・権限管理

### 🛡️ アクセス制御
- **管理者権限**: 全機能アクセス可能
- **編集者権限**: 記事作成・編集のみ
- **閲覧者権限**: プレビューのみ

### 🔑 API キー管理
```bash
# 環境変数確認
echo $SANITY_API_TOKEN
echo $NEXT_PUBLIC_SANITY_PROJECT_ID

# 権限レベル確認
curl -H "Authorization: Bearer $SANITY_API_TOKEN" \
  https://qier3tei.api.sanity.io/v1/projects/qier3tei
```

### 📊 監査ログ
- **変更履歴**: Sanity Studio内で確認
- **アクセスログ**: Sanity プロジェクト設定
- **API使用量**: ダッシュボードで監視

---

## 🎯 自動化・効率化

### 🤖 自動投稿システム
```javascript
// 定期投稿スケジュール例
const schedule = {
  "weekly": "毎週月曜日 9:00",
  "monthly": "毎月1日 10:00",
  "seasonal": "季節イベント前2週間"
}
```

### 📝 テンプレート活用
```json
{
  "eventReport": {
    "title": "[イベント名] 開催報告",
    "category": "活動報告",
    "template": "event-report-template"
  },
  "announcement": {
    "title": "[重要] お知らせ",
    "category": "お知らせ",
    "template": "announcement-template"
  }
}
```

### 🔄 バッチ処理
```bash
# 複数記事一括処理
node batch-update-posts.js

# 画像一括最適化
node optimize-all-images.js

# メタデータ一括更新
node update-all-metadata.js
```

---

## 📚 開発者向けリファレンス

### 🔗 Sanity スキーマ定義
```javascript
// schemas/post.js
export default {
  name: 'post',
  title: 'Post',
  type: 'document',
  fields: [
    {
      name: 'title',
      title: 'Title',
      type: 'string',
      validation: Rule => Rule.required()
    },
    {
      name: 'slug',
      title: 'Slug',
      type: 'slug',
      options: {
        source: 'title',
        maxLength: 96,
      }
    },
    {
      name: 'body',
      title: 'Body',
      type: 'blockContent'
    }
  ]
}
```

### 🔌 API エンドポイント
```javascript
// ブログ記事取得
const posts = await client.fetch(`
  *[_type == "post" && publishedAt < now()] 
  | order(publishedAt desc)
`)

// 特定記事取得
const post = await client.fetch(`
  *[_type == "post" && slug.current == $slug][0]
`, { slug })
```

### 🎨 カスタムコンポーネント
```javascript
// components/PortableText.js
const components = {
  types: {
    image: ({value}) => (
      <img 
        src={urlFor(value).width(800).url()} 
        alt={value.alt} 
      />
    )
  }
}
```

---

## 📞 サポート・トラブルシューティング

### 🆘 緊急連絡先
- **技術サポート**: Claude Code引継ぎドキュメント参照
- **Sanity サポート**: https://www.sanity.io/help
- **Next.js サポート**: https://nextjs.org/docs

### 🔍 ログ確認方法
```bash
# アプリケーションログ
tail -f logs/app.log

# Sanity同期ログ
tail -f logs/sanity-sync.log

# エラーログ
tail -f logs/error.log
```

### 📊 健全性チェック
```bash
# システム全体チェック
node health-check.js

# データベース接続テスト
node test-db-connection.js

# パフォーマンステスト
node performance-test.js
```

---

## 🎉 成功指標・KPI

### 📈 測定すべき指標
- **記事公開頻度**: 月間投稿数
- **コンテンツ品質**: 平均文字数・画像使用率
- **システム安定性**: エラー率・ダウンタイム
- **ユーザー体験**: ページ読み込み速度

### 🎯 目標設定
```json
{
  "monthly_posts": 4,
  "average_words": 1500,
  "image_ratio": 0.8,
  "error_rate": "<1%",
  "load_time": "<3s"
}
```

---

**📅 作成日**: 2025年11月7日  
**✏️ 作成者**: Claude Code  
**🏷️ バージョン**: v1.0.0  
**🔄 引継ぎ先**: CODEX  
**📝 更新予定**: 運用開始後フィードバック反映